services:
  web:
    build: ./frontend
    container_name: tasfam-web
    ports:
      - "7070:3000"
    environment:
      - DATABASE_URL=postgresql://${DB_USER:-tasfam}:${DB_PASSWORD:-MyTasfam@2026}@db:5432/tasfam
      - JWT_SECRET=${JWT_SECRET:-xZ5dc9P2LTVWGb2X/+liSO5bU7YoDMCySYRrM5dGH9o=}
    volumes:
      - uploads:/app/public/uploads
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    container_name: tasfam-db
    ports:
      - "5436:5432"
    environment:
      - POSTGRES_DB=tasfam
      - POSTGRES_USER=${DB_USER:-tasfam}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-MyTasfam@2026}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-tasfam} -d tasfam"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  pgdata:
  uploads:
